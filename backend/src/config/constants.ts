/** Вартість хешування bcrypt. Одна для всіх місць, де створюється пароль. */
export const SALT_ROUNDS = 10;

/** Максимальний розмір JSON-тіла запиту. Найбільші легітимні тіла — bulk-оцінки й явка групи. */
export const JSON_BODY_LIMIT = '1mb';

/**
 * Пояс, у якому живе академія. Сервер на хостингу працює в UTC, тож скрипти, що
 * самі ставлять час занять (seed), рахують його тут, а не через setHours: інакше
 * «заняття о 18:00» ставало б 18:00 UTC — 21:00 у Києві.
 */
export const ACADEMY_TIME_ZONE = 'Europe/Kyiv';
